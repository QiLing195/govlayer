# V6.3 语义检索 POC 报告

> 回答的问题：**当用户不按制度原文的措辞提问时，检索还能不能找到那一条？**
> 这是本项目已知的弱点（身份层"措辞远离训练"时泛化有限），
> 也是 toB 客户实际提问方式的常态。

## 一句话结论

**纯关键词/字符重叠检索在"措辞远离"时几乎完全失效（1/9 = 11%）；
换成本地 ONNX int8 语义编码器后达到 7/9 = 78%。**

同时，**在"措辞贴近"时两者一样好（都是 8/8），而语义编码器的排名反而略差**
（平均排名 1.25 vs 字符重叠 1.0）——因此正确架构是**混合检索**，
而不是用语义全面替换关键词。

---

## 一、方法学：为什么必须分组测量

本次最重要的不是数字，而是**测量方式**。之前的评测（`eval_semantic_gov.py` 的
9 条、`eval_ann_index.py` 的 7 条手写问题）用的问题**全都含有条款关键词**：
"矿工几天会被劝退""忘记打卡怎么办""请假需要什么材料"。

**用这类问题测语义检索，等于让语义模型和关键词检索站在同一条起跑线上比。**
关键词检索在这种题上本来就是对的，于是只能得到"两边都很好"的结论——
语义的价值恰好被测量方式掩盖掉了。

真实用户不会照着制度原文提问。真实提问是这样的：

| 真实提问 | 制度原文 |
|---|---|
| 一个月不来上班几天会被开除？ | 当月旷工5天或全年累计7天**予以劝退** |
| 身体不舒服需要休息该走什么手续？ | **病假**需县市级医院证明 |
| 要去外地办事需要提前打招呼吗？ | **出差**需出差前**报备** |

**三对里没有一对共享关键词。** 因此本报告把查询集拆成两组：

- **措辞贴近**（8 条）：含目标条款关键词，作为对照组；
- **措辞远离**（9 条）：刻意避开该条款关键词表，作为真正的考题。

分组是**可验证的性质**，不是作者的口头声明，由
`tests/test_eval_query_sets.py` 强制：
贴近组每条**必须**含至少一个关键词，远离组每条**不得**含任何关键词。

> 这条测试当场抓到过真实问题：原查询「早上来得太晚会被扣多少钱？」含有
> penalty 的关键词「扣」，它**并不远离**，会略微抬高字符重叠的成绩。
> 修正后重测，语义成绩从 8/9 降到 7/9——被挤掉的正是那一分"蹭"来的命中。

---

## 二、配置

| 项 | 值 |
|---|---|
| 语料 | 7 条真实制度条款 + 1 条仅 level 2 的秘密条款 + **300 条同域 HR 干扰条款** = 308 条 |
| 检索 | **暴力精确检索**（不走 ANN）——把"编码器质量"与"索引近似误差"彻底分开 |
| top-k | 5 |
| 权限级别 | 0（最低） |
| 编码器 A | `hashing-blake2b`，dim=256，字符 n-gram 稳定哈希（**无语义能力**，作为关键词检索的替身） |
| 编码器 B | `bge-small-zh-v1.5` **ONNX int8**，dim=512（24 MB，本地推理，无需 torch） |

**为什么干扰条款要 300 条**：库只有 7 条时 top-5 几乎全中，测不出任何东西。
干扰条款是 HR 同域主题（报销/加班/社保/试用期/年假…），且其主题词不出现在"针"里，
这样"请假"与"报销"之间必须真的区分开。

**为什么用暴力精确检索**：ANN 会给结果引入近似误差，混淆"是编码器找不到"还是
"是索引漏了"。本次要单独回答编码器的问题，所以走精确路径。

---

## 三、结果

| 检索方式 | 措辞贴近 | 措辞远离 | 命中时平均排名（远离组） |
|---|---|---|---|
| 字符重叠（哈希占位） | 8/8（100%） | **1/9（11%）** | 3.0（仅 1 条命中，不具代表性） |
| **ONNX 语义（int8）** | 8/8（100%） | **7/9（78%）** | 2.29 |

**措辞远离组：语义比字符重叠多命中 6 条（1/9 → 7/9）。**

两个必须同时读的读数：

1. **语义的真正价值在"措辞远离"**：11% → 78%。这是关键词检索失效的地方。
2. **语义在"措辞贴近"上并没有更好**：命中数相同（8/8），
   而**平均排名从 1.0 退到 1.25**——精确关键词命中时，字符匹配反而更"稳、准、狠"。
   嵌入会把相邻概念抹匀，导致本该排第一的条款偶尔掉到第二。

---

## 四、失败案例（不隐藏）

语义编码器仍有 2 条未进 top-5：

| 查询 | 期望条款 | 制度原文 |
|---|---|---|
| 早上来得太晚有什么后果？ | `penalty` | 迟到早退每次扣日工资50元 |
| 要去外地办事需要提前打招呼吗？ | `travel` | 出差需出差前报备 |

第一条在修正"扣"字泄漏前是命中的——修正后失败，说明它当时很可能是**靠那个字**
而非靠语义。这恰好说明：**修掉测量缺陷会让成绩下降，而那个下降后的数字才可信。**

第二条（"打招呼" ↔ "报备"）属于口语到书面语的跨度，是当前模型的真实边界。

---

## 五、架构含义（本次最有实用价值的推论）

### 5.1 应当做混合检索，而不是语义全面替换

| 查询类型 | 更优的检索方式 |
|---|---|
| 含精确术语（"旷工""请假条""8:30"） | **关键词/字符匹配**（排名更准，且完全确定、可解释） |
| 口语化、无术语重叠 | **语义编码器**（关键词基本失效） |

建议：两路召回 → 合并去重 → 排序。关键词路保"精确与可解释"，
语义路保"口语召回"。**纯语义会牺牲前者，纯关键词会牺牲后者。**

### 5.2 检索可以本地化，覆盖判定不行

当前服务用 `LLMSemanticBackend`（DeepSeek API）同时做**相关性匹配**与**覆盖判定**。
本次结果表明：**相关性匹配这一半，一个 24 MB 的本地 int8 模型就能胜任**——
意味着检索可以不出内网、无 per-query 费用、可离线运行。

但**覆盖判定**（"制度里到底有没有规定这件事""缺的是哪一部分"）是更难的判断，
本轮**没有测**，仍依赖 LLM。不要把这两个能力混为一谈。

### 5.3 交付体积影响可控

`onnxruntime` + `tokenizers` + 24 MB 模型，与 torch 的 GB 级不是一个量级，
"镜像小、可内网离线"这条卖点**没有破**。
（具体镜像体积本轮**未实测**，不在此给数字。）

---

## 六、限制（引用以上数字必须一并说明）

1. **措辞远离组只有 9 条，样本量太小。** 1/9 → 7/9 的差距方向明确且很大，
   但 95% 置信区间仍然很宽，**不应作为精确估计**。扩充查询集是首要后续工作。
2. **干扰条款为模板合成**（"主题+动作"），与真实制度的措辞分布有差距；
   真实语料的区分难度可能更高。
3. **"措辞远离"≠ 零字重叠。** 中文里仍可能有个别字（"休""需"）同时出现在
   问题与条款中，所以字符重叠检索未必全错——这正是必须分组、不能只看总分的理由。
4. **只测了编码器检索**，不含 GovLayer 的字段级权限、空白判定与先例逻辑。
5. **只测了一个模型。** 未与其他中文小模型（text2vec、m3e 等）对比，
   也未测不同量化精度（int8 vs fp32）的差异。
6. **模型来自社区转换版**（Xenova/bge-small-zh-v1.5），非 BAAI 官方 ONNX 导出
   （BAAI 仓库的 `onnx/` 路径实测 404）。**私有化交付时应校验模型哈希**，
   明确"交给客户的是什么"。
7. **查询侧加了 BGE 官方建议的指令前缀**（"为这个句子生成表示以用于检索相关文章："），
   文档侧不加——这是非对称检索的正确用法，会影响结果，属于实现细节需一并说明。

---

## 七、后续工作

- [x] 把编码器接进 `GovLayer.retrieve()`（已接入：关键词 + 语义 RRF 混合，见第八节）
- [x] 分组可验证（贴近/远离由测试强制）
- [x] 阈值标定（已用真实数据标定为 0.51，见第八节）
- [ ] **扩充措辞远离查询集**（9 → 50+），把置信区间收窄到可引用的程度
- [ ] **接真实企业制度语料**复核（当前为 7 条手写条款 + 模板合成干扰项）
- [ ] **覆盖判定本地化**——本报告最重要的未解问题，见第八节
- [ ] 加 reranker / 换更强编码器，看能否拉开"相关 vs 同域无关"的分数间隔
- [ ] 多模型 / 多量化精度对比

---

## 八、补测：阈值标定，以及一个比阈值重要得多的负结果

接入服务后实测暴露：默认阈值 `0.25`（**猜的**）太低，问"公司食堂几点开饭？"竟被判成
`restricted`，系统回答"制度中有相关规定，但超出你的角色权限范围"——**一句会被当场戳穿的假话**。

用 `calibrate_threshold.py` 在真实数据上标定（`gov_employee_rules.json` 7 条 + bge-small-zh int8）：

| | top-1 最低 | 中位 | 最高 |
|---|---|---|---|
| 相关问题（17 条） | 0.4971 | **0.6135** | 0.7211 |
| 无关问题（15 条，同域但制度无规定） | 0.4025 | **0.4507** | **0.5026** |

**严格可分不成立**（尾部重叠仅 0.0055）。但存在"零无关误纳"窗口 `(0.5026, 0.5235]`，
窗口内相关召回 **15/17（88%）**；现取 **0.51**。代价是漏掉 2 条措辞最远的问法。

> 这同时说明"能不能严格分开"是个**过于粗糙**的判据：中位数差 0.16、尾部只重叠 0.0055 时，
> 正确结论是"有可用工作窗口"，而不是"分不开"。最初的脚本只会报二分结论，是**脚本的缺陷**。

### 更重要的负结果：相似度 ≠ 覆盖

无关问题的 top-3 **几乎总是同几个条款**（leave / penalty / work-hours / stats）。
原因不是模型差，而是这些问题是**同域**的："有加班费吗？""调岗需要本人同意吗？"
与现有考勤条款本来就在语义上邻近，模型给出 0.45~0.50 的相似度是**合理的**。

**因此：阈值只能过滤"完全不相关"，无法判断"相关但制度没规定"。**
而后者正是 `gap`（知识空白升级人工）——它需要的是**覆盖判定**，不是相似度。

这实测支撑了本报告 5.2 节的判断：**检索可以本地化，覆盖判定不行**。落地结论：

| 配置 | 检索 | 覆盖判定 | 能诚实说"制度没规定"吗 |
|---|---|---|---|
| `auto` + LLM | LLM（**内容出内网**） | LLM | ✅ |
| **`local` + LLM** | **本地（不出内网）** | LLM | ✅ ← **推荐** |
| 纯本地（无 LLM） | 本地 | 6 条关键词探针（无规则即视为覆盖） | ❌ |

**纯本地配置可以演示权限隔离与检索，但不能用来宣称"知识空白识别"。**

---

## 附：复现命令

```powershell
# 零依赖基线（无需模型）：量化"关键词检索在措辞远离时有多差"
python eval_semantic_retrieval.py

# 装本地语义编码器并对比
python -m pip install -r requirements-ann.txt
python fetch_embedding_model.py --endpoint https://hf-mirror.com
$env:GOVLAYER_ONNX_MODEL="E:\deepseek\c-former\models\bge-small-zh-v1.5-int8"
python eval_semantic_retrieval.py --compare

# 查询集不变量测试（贴近/远离是强制性质，不是我说的）
python -m pytest tests/test_eval_query_sets.py -q
```

明细数据：`artifacts/semantic_retrieval_eval.json`（不入库）

---

**结论一句话**：关键词检索在真实提问方式下基本不可用（11%），
本地 24 MB 语义模型能把它拉到 78%，但**它并不是全面更好**——
精确术语场景下关键词仍然更准。所以这不是"换个模型"的问题，而是**换架构**：
两路召回，各管一段。
