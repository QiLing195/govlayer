# V6.3 真实语料检索与阈值标定报告

> 用**四份语料**（三份真实文档 + 一份手写）回答三个问题：
> 相似度下限该设多少？它能挡住什么？真实语料上的检索到底什么水平？

## 一句话结论

**没有任何单一相似度阈值能同时适用于四份语料**（实测建议值 0.499 / 0.535 / 0.557 / 0.567）
→ **阈值必须逐库标定，不能作为代码常数存在。**

阈值能挡住"**完全跑题**"的问题（但效果随语料差异极大：94%/95% vs 57%/75%），
**挡不住"问的是本领域但制度没规定"的问题**——而后者才是真实员工最常问的。

---

## 一、四语料对照（全部实测）

| 数据集 | 条数 | 来源 | 主题 | 相关中位 | 远域 max | 同域 max |
|---|---|---|---|---|---|---|
| `employee_rules` | 7 | 手写（合成） | 泛 HR | 0.6135 | 0.4983 | 0.5026 |
| `registration` | 7 | **真实**（迎新报到） | 单一流程 | 0.5669 | 0.5266 | **0.5770** |
| `renshi` | 43 | **真实**（人事管理条例） | 泛 HR | 0.5924 | 0.5551 | 0.5544 |
| `kaohe` | 50 | **真实**（考核规定） | **单一主题** | 0.6154 | 0.5542 | **0.6869** |

**零误纳窗口与代价：**

| 数据集 | 远域组建议阈值 | 相关召回 | 同域组建议阈值 | 相关召回 |
|---|---|---|---|---|
| `employee_rules` | 0.499 | **16/17（94%）** | 0.513 | 15/17（88%） |
| `registration` | 0.535 | **8/14（57%）** | 0.584 | 7/14（50%） |
| `kaohe` | 0.557 | **19/20（95%）** | 0.692 | **7/20（35%）** |
| `renshi` | 0.567 | 15/20（75%） | 0.567 | 15/20（75%） |

建议值跨度 0.499~0.567，**没有交集**。

---

## 二、三条修正后的结论

### 2.1 阈值必须逐库标定（最强的一条）

现行代码里的 `DEFAULT_MIN_SCORE = 0.51` 是**在单份语料（employee_rules）上标出来的**。
本次四语料实测显示它对其中的 `registration`（需 0.535）与 `renshi`（需 0.567）**偏低**，
即会放进本应被挡住的远域问题。

> **"能不能用一个默认值"这个问题，答案是不能。** 交付方式应改为：
> 部署时必做 `calibrate_threshold.py`，把结果写进 `GOVLAYER_SEMANTIC_MIN_SCORE`。

### 2.2 「同域未覆盖比远域更接近相关」**不是普遍规律**（修正了我上一轮的判断）

| 数据集 | 同域 max − 远域 max |
|---|---|
| `kaohe` | **+0.133**（严重） |
| `registration` | +0.050 |
| `employee_rules` | +0.004 |
| `renshi` | **−0.001**（同域甚至更低） |

`kaohe` 严重的原因是**它把"后果"规定得极细**（考核结果 → 薪级工资/岗位等级/聘用合同），
于是"考核不合格会影响**退休待遇**吗""会记入**个人信用**吗""能换**奖金**吗"这类
**同类外推**问题，与已覆盖条款贴得极近。

而 `renshi` 的同域问题（工伤/公积金/年假）属于**条例中压根没提的其他制度域**，
模型找不到邻近内容，得分自然低。

**所以驱动因素不是"主题广度"，而是「未覆盖问题是否属于已覆盖事项的同类外推」。**

> 这也说明：**制度写得越全、越细，"空白识别"反而越难做对**——
> 因为没写的部分恰恰是最贴近已写部分的那些。这与直觉相反，也是最需要向客户说明的一点。

### 2.3 语料自身的"远域"是相对的，不是绝对属性

`registration` 的远域组只到 **57%** 召回，是四份里最差的。
原因：它 7 条全是"学校事务"，而远域问题（图书馆、校园卡、校历、转专业）**也是学校事务**。
**7 条语料覆盖不了"学校"这个大领域**，所以任何学校相关的问题都跟那 7 条很像。

**含义**：`off_domain` 不是问题的固有属性，而是"相对于这份语料有多远"。
跨语料直接比较远域分数是不严谨的。

---

## 三、检索质量：真实语料上的真实水平

| 数据集 | 条数 | top-1 命中 | top-3 命中 | 随机基线(top-1) | 相对随机 |
|---|---|---|---|---|---|
| `employee_rules` | 7 | **71%** | **100%** | 14% | 5× |
| `registration` | 7 | **71%** | 71% | 14% | 5× |
| `renshi` | 43 | 50% | 85% | 2.3% | 22× |
| `kaohe` | 50 | **25%** | **50%** | 2.0% | 12× |

两个必须一起读的读数：

1. **相对随机基线，大语料上模型表现更好**（`kaohe` 的 25% 是随机的 12 倍）——
   模型没有坏，只是候选变多了。
2. **绝对准确率随语料增大而下降**，而在产品里**只有绝对值有意义**：
   `kaohe` 上 top-1 只有 25%，意味着"系统找到了那一条"这个说法在密集制度上**不成立**。

`kaohe` 为何最差：第 11~14 条（优秀 / 合格 / 基本合格 / 不合格的条件）
**结构几乎完全相同**，模型难以区分。

> **产品含义：不能给单一答案。** 必须靠 top-k + 覆盖判定，
> 而不是把某一条当成"系统找到的答案"直接呈给用户。

---

## 四、这一轮我自己修正过的错误（留档）

| # | 错误 | 后果 |
|---|---|---|
| 1 | 把两档无关问题**混在一起统计** | **把最重要的发现藏起来了**：混着看只能说"分不开"，分开看才发现远域组与相关组几乎完美可分。这不是 bug，是**测量设计错误** |
| 2 | 关键词按**长度**降序排序 | "降低岗位等级"挤掉"**处分**"，导致 `renshi` 第29条的"贴近"查询失效 |
| 3 | 关键词按**文档频率**升序排序 | "考核工作领导小组"挤掉本文中心词"**年度考核**"，导致 `kaohe` 第17条失效 |
| 4 | 不变量写死"目标必须在 level 0 可见" | 限制了为**受限条款**写查询（而标定用的是跨级别检索） |

第 2、3 条是同一个毛病：**用单一判据去解决本质上是多目标的问题**。
最终采用「本文内出现次数降序 → 文档频率升序 → 长度降序」。

**关键词排序改了三轮，每一轮都是被测试逼出来的。** 而真正的判据——
"员工实际会怎么问"——**不在这份文档里，在业务方脑子里**。
所以 `build_gov_regulation.py` 的输出**始终需要人工过一遍**，
这句话在交付时不能省。

---

## 五、限制（引用以上数字必须一并说明）

1. **`renshi` 有一个无法分离的混淆变量**：它平均每条 **69 字**，`kaohe` 是 **143 字**。
   主题广度与条款长度被绑在一起，**因此无法干净归因**。
   要分离需再找"主题集中但条款短"或"主题泛但条款长"的文档。
2. **样本量仍偏小**：每份语料的"相关"查询 14~20 条、"同域未覆盖"5~9 条。
   零误纳窗口的宽度（如 `kaohe` 的 (0.6869, 0.6963]）精度有限。
3. **`registration` 只有 level 0/1 两级**，与另两份的三级不同。
4. **两份法规的分层是提议值**，不是真实权限边界——它们是**公开发布的规范性文件**，
   绝大部分内容本就应该公开。**本报告不能用于宣称"权限隔离已通过真实场景验证"。**
5. **只测了一个模型**（bge-small-zh-v1.5 int8），未与其他中文小模型或量化精度对比。
6. **未测真实客户语料**。三份文档都是公开法规/流程，与"企业真实制度"在措辞、
   冗余度、内部术语上仍有差距。

---

## 六、落地建议

1. **阈值逐库标定**，写进 `GOVLAYER_SEMANTIC_MIN_SCORE`；代码里的默认值只作兜底，
   且服务启动时应提示"未标定"。
2. **不要给单一答案**：返回 top-k 并配合覆盖判定，尤其在条款同构的密集制度上。
3. **覆盖判定必须保留 LLM**（或训练替代品）：
   `GOVLAYER_RETRIEVAL=local` 让检索不出内网，但覆盖判定仍需要它得出 `gap`；
   纯本地配置**无法诚实地说"制度没规定"**。
4. **接新客户时，"为它写一套评测查询集"是必做成本**，不是可选项——
   `eval_query_sets.py` 里那四组问题，就是每次都要重做一遍的东西。

---

## 附：复现命令

```powershell
python build_gov_regulation.py --name all
python -m pytest -q
$env:GOVLAYER_ONNX_MODEL="models\bge-small-zh-v1.5-int8"
python calibrate_threshold.py --dataset data/gov_kaohe.json
python calibrate_threshold.py --dataset data/gov_renshi.json
python calibrate_threshold.py --dataset data/gov_employee_rules.json
python calibrate_threshold.py --dataset data/gov_registration.json
```

---

**结论一句话**：检索在真实语料上是**可用的但远非可靠**（top-1 25%~71%）；
相似度阈值**必须逐库标定**且只能挡住跑题问题；
"制度空白识别"在技术上依赖覆盖判定，而**制度写得越全，它越难做对**。
